# 배포 시 호스팅(Vercel/Cloudflare Pages) 환경변수에 넣는 값.
# 개발 중에는 비워두면 됩니다(vite proxy가 localhost:8000으로 넘김).
# 배포 시에는 Render 백엔드 주소를 넣으세요. 예:
# VITE_API_BASE=https://jusikdo-ai-backend.onrender.com
VITE_API_BASE=
